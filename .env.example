# Instagram credentials (required)
INSTAGRAM_USERNAME=your_username_here
INSTAGRAM_PASSWORD=your_password_here

# CSV file path (optional, defaults to ./users_to_unfollow.csv)
CSV_FILE_PATH=./users_to_unfollow.csv

# Run browser in headless mode (optional, defaults to false for visibility)
HEADLESS=false
